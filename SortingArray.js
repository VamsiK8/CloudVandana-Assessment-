// Sample array
var array = [1, 2, 3, 4, 5, 6, 7];

// Sort the array in descending order
var sortedArray = array.sort((a, b) => b - a);

console.log(sortedArray);// Sorted array in descending order